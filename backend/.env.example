# Server
PORT=5000
NODE_ENV=development

# CORS — comma separated list of allowed origins in production
CLIENT_ORIGIN=http://localhost:5000

# Email (used by controllers/contactController.js via Nodemailer)
# SMTP credentials for sending enquiry notifications to the sales team.
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=your-email@example.com
SMTP_PASS=your-app-password
NOTIFY_EMAIL=namratha@stcliving.in

# WhatsApp click-to-chat number (E.164 without +)
WHATSAPP_NUMBER=919100098038
